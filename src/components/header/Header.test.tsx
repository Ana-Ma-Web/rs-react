// import React from 'react';
// import { cleanup, render, screen } from '@testing-library/react';
// import Header from './Header';

it('should have anime', () => {
  // render(<Header />);
  // const message = screen.queryByText(/anime/i);
  // // const details = screen.queryByTestId('details');
  // // if (!details) {
  // // } else expect(message).toBeNull();
  // expect(message).toBeVisible();
  // afterEach(cleanup);
});

it('should match error button', () => {
  // // screen.debug();
  // render(<Header />);
  // const errorBtn = screen.queryByText(/error button/i);
  // expect(errorBtn).toMatchSnapshot();
  // afterEach(cleanup);
});
